export function exportLeadsCsv(leads:any[]){const headers=['name','type','category','city','state','phone','website','score','priority','status','issues'];const rows=leads.map(l=>headers.map(h=>`"${String(h==='issues'?(l.websiteIssues||[]).join('; '):l[h]??'').replaceAll('"','""')}"`).join(','));const csv=[headers.join(','),...rows].join('\n');const blob=new Blob([csv],{type:'text/csv'});const url=URL.createObjectURL(blob);const a=document.createElement('a');a.href=url;a.download='cptwebs-leads.csv';a.click();URL.revokeObjectURL(url)}
