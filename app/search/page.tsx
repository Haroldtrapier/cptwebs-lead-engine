'use client'
import {useState} from 'react'
export default function SearchPage(){const [done,setDone]=useState(false);return <div className="stack"><div><h1 className="title">Lead Search</h1><p className="subtitle">MVP search uses mock data now. Later this connects to Google Places, PageSpeed Insights, SerpAPI, Apify, and Supabase.</p></div><div className="card"><div className="form-grid"><label className="label">City<input className="input" defaultValue="Charlotte"/></label><label className="label">State<input className="input" defaultValue="NC"/></label><label className="label">Radius<select className="input" defaultValue="25"><option>10</option><option>25</option><option>50</option></select></label><label className="label">Lead type<select className="input"><option>Both</option><option>Church</option><option>Small Business</option></select></label><label className="label">Business category<input className="input" defaultValue="Churches, salons, contractors, local services"/></label><label className="label">Target count<input className="input" defaultValue="200"/></label></div><p className="footer-note">Compliance: use public business information only and avoid spam automation.</p><button className="btn" onClick={()=>setDone(true)}>Run mock search</button>{done?<div className="copybox" style={{marginTop:16}}>Mock search complete. Review the generated sample leads on the Leads page.</div>:null}</div></div>}
